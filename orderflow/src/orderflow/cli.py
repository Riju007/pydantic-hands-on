"""
Phase 7 (stretch) — CLI Wrapper

TODO: A simple CLI so the pipeline can be run like:

    orderflow validate data/sample_input/mobile_orders.json

Wire together: read raw JSON -> validate against source model ->
normalize() -> collect into ValidationReport -> print/save summary.
"""

# import typer
#
# app = typer.Typer()
#
# @app.command()
# def validate(path: str):
#     ...
#
# if __name__ == "__main__":
#     app()
